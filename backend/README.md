# FoodFlow backend

See the root [`README.md`](../README.md) for XAMPP, environment, migration, seed, test, and run instructions. This directory contains the Django REST API; credentials belong in `.env`, copied from `.env.example`.
