-- Data is generated safely through Django so password hashing and relationships remain valid.
-- Run: cd backend && python manage.py seed_data
USE food_delivery_db;
SELECT 'Run python manage.py seed_data to load demo records' AS instructions;
